1) create a tf infra pipeline to create
Resource group
Azure data table (table api)
keyvault
application insights
azure function 
2) migrate from sqlite to azure data table.
3) create a report cli to do the report with console logs
4) create a reportservice to do the report to a json and that will be consumed by the frontend
5) investigate how is architected a node api
do a express api with the project
also, that should comply with the cli
cli calls the api and then show a report with console logs.
5) move the documentation MDs in another folder.
6) update the claude.md
